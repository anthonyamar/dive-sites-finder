<div class="container px-4 mx-auto mt-8 sm:mt-0 sm:mb-28">
  <h2 class="heading-1">
    What to know about <%= @city.country.name %>?
  </h2>
  <div class="mt-10 w-full p-2 bg-white rounded-lg shadow sm:p-6">
    <div class="grid grid-cols-2 sm:grid-cols-4 gap-8 sm:gap-12">
      <% if @city.country.languages.present? %>
        <div class="text-center text-2xl">
          <span class="font-serif text-main-sky">Languages</span><br>
          <span class="text-xl"><%= to_sentence(@city.country.languages) %></span>
        </div>
      <% end %>
      <% if @city.country.demonym.present? %> 
        <div class="text-center text-2xl">
          <span class="font-serif text-main-sky">Demonym</span><br>
          <span class="text-xl"><%= @city.country.demonym %></span>
        </div>
      <% end %>
      <% if @city.country.currency.present? %>
        <div class="text-center text-2xl">
          <span class="font-serif text-main-sky">Currency</span><br>
          <span class="text-xl"><%= @city.country.currency %></span>
        </div>
      <% end %>
      <% if @city.country.pressure.present? %>
        <div class="text-center text-2xl">
          <span class="font-serif text-main-sky">Pressure</span><br>
          <span class="text-xl"><%= @city.country.pressure %></span>
        </div>
      <% end %>
      <% if @city.country.phone_prefix.present? %>  
        <div class="text-center text-2xl">
          <span class="font-serif text-main-sky">Phone prefix</span><br>
          <span class="text-xl"><%= @city.country.phone_prefix %></span>
        </div>
      <% end %>  
      <% if @city.country.voltage.present? %>  
        <div class="text-center text-2xl">
          <span class="font-serif text-main-sky">Voltage</span><br>
          <span class="text-xl"><%= @city.country.voltage %></span>
        </div>
      <% end %>
      <% if @city.country.plug.present? %>  
        <div class="text-center text-2xl">
          <span class="font-serif text-main-sky">Plug</span><br>
          <span class="text-xl"><%= @city.country.plug %></span>
        </div>
      <% end %>
      <% if @city.country.international_airports.present? %>
        <div class="text-center text-2xl">
          <span class="font-serif text-main-sky">International airports</span><br>
          <span class="text-xl"><%= to_sentence(@city.country.international_airports) %></span>
        </div>
      <% end %>
    </div>
  </div>
</div>